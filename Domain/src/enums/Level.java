package enums;

/**
 * This enum is used by the User and Exercise classes.
 * It is used to set a user's experience and an exercise's difficulty.
 */
public enum Level {
    EASY,
    MEDIUM,
    HARD
}
